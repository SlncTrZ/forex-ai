symbols:
  XAUUSD:
    enabled: true
    aliases: [XAUUSD, GOLD]
    timeframes: [M5, M15, H1, H4]
  EURUSD:
    enabled: true
    aliases: [EURUSD]
    timeframes: [M5, M15, H1, H4]

# Actual broker symbol names are discovered after account login and should be
# persisted separately; do not hard-code suffixes until the broker is known.
